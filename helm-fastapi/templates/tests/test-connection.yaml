apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-fastapi.fullname" . }}-test-connection"
  labels:
    {{- include "helm-fastapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-fastapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
